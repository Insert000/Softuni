function solve(input) {
    let name = input[0];
    let pesho = input[1];
    let age = input[2];
    let realAge = input[3];
    let gender = input[4];
    let male = input[5];
console.log(`{ ${name}: '${pesho}', ${age}: '${realAge}', ${gender}: '${male}' }`);
}
solve(['name', 'Pesho', 'age', '23', 'gender', 'male']);

