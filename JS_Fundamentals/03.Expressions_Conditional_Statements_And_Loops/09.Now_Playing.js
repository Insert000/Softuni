function solve(input) {
    let number = input[0];
    let name = input[1];
    let time = input[2];
    console.log(`Now Playing: ${name} – ${number} [${time}]`);
}
solve(['Number One', 'Nelly', '4:09']);